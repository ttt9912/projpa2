---------------------------------------------------------
On @Entity
---------------------------------------------------------
standard:
    @Getter
    @Setter
    @ToString(exclude = "")

optional:
    @AllArgsConstructor
    @NoArgsConstructor