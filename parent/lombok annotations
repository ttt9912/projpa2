---------------------------------------------------------
On @Entity
---------------------------------------------------------
standard:
    @Getter
    @Setter
    @ToString(exclude = "")   basically exclude @OneToMany properties

optional:
    @AllArgsConstructor
    @NoArgsConstructor